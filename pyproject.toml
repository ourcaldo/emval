[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Self-hosted bulk email validator with HTTP DNS API"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "pyyaml>=6.0",
]
